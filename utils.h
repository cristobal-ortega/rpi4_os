#ifndef __UTILS_H__
#define __UTILS_H__

// TODO: investigate why no inline is allowed in these declarations
void set_address_to(unsigned long long address, unsigned int value);
int get_value_from(unsigned long long address);

#endif // __UTILS_H__